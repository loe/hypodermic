Opens a .docx file and returns a plain text string of the contents and optionally returns the thumbnail.

include 'hypodermic'

doc, thumbnail = Hypodermic.extract('path/to/document', :thumbnail => true)